var EV_CLICK = 'click',
	win = window,
	FastClick = win.FastClick,
	hasFastClick = !!FastClick,
	hasTouch = !!('ontouchstart' in win || navigator.maxTouchPoints);

export default {
	bind(el, value) {

	}
};
